// Stub for @coinbase/cdp-sdk, an optional server side dependency of the Coinbase/Base wallet connector that pulls in
// packages (@x402/*, @solana/kit) we never use. Nothing in AttestCredit touches it at runtime.
export class CdpClient {}
const empty = {};
export default empty;
